usda plants API
===============

USDA plants database API

## routes

- `/` (redirects to `/heartbeat`)
- `/heartbeat`
- `/search`

## params

on `/search` route only

- `fields`, e.g., `fields='Genus,Species'`
- `limit`, e.g., `limit=5`
